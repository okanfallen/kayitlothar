import { GuildMember } from "discord.js";

interface Colors {
    blue: number,
    darkblue: number,
    cyan: number,
    lightblue: number,
    brown: number,
    darkred: number,
    red: number,
    lightred: number,
    orange: number,
    darkorange: number,
    darkyellow: number,
    yellow: number,
    lightyellow: number,
    beige: number,
    gold: number,
    green: number,
    darkgreen: number,
    lightgreen: number,
    yellowgreen: number,
    lightpink: number,
    darkpurple: number,
    pink: number,
    silver: number,
    lightgrey: number,
    grey: number,
    black: number,
    white: number,
    purple: number,
    // DISCORD
    embed: number,
    discord: number
}

export function codeGenerator(length: number): string;
export let colors: Colors;
export function toStringDate(date: string, lang: 'fr' | 'en'): string | boolean;
export function toIntDate(date: string): number;
export function fromIntToDate(timestamp: number, lang: 'fr' | 'en'): string;
export function rankCardGenerator(emptyFill: string, barFill: string, level: number, xp: number, nextLevelXP: number, member: GuildMember)